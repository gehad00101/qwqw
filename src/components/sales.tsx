"use client";

import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export function Sales() {
  return (
    <Card>
        <CardHeader>
            <CardTitle>إدارة المبيعات</CardTitle>
        </CardHeader>
        <CardContent>
            <p>سيتم بناء صفحة إدارة المبيعات هنا.</p>
        </CardContent>
    </Card>
  );
}
